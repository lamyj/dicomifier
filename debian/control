Source: dicomifier
Maintainer: Julien Lamy <lamy@unistra.fr>
Section: science
Priority: optional
Build-Depends: debhelper (>= 10), 
               d-shlibs,
               dh-python,
               cmake,
               pkg-config,
               libboost-date-time-dev, 
               libboost-filesystem-dev, 
               libboost-regex-dev, 
               libboost-system-dev, 
               libjsoncpp-dev, 
               liblog4cpp5-dev, 
               libboost-python-dev, 
               libodil-dev (>= 0.9.2),
               python-dateutil,
               python3-dateutil,
               python-dev,
               python3-dev,
               python-numpy,
               python3-numpy,
               python-odil (>= 0.9.2),
               python3-odil (>= 0.9.2),
               python-nibabel,
               python3-nibabel,
               zlib1g-dev,
               libboost-test-dev,
               python-nose,
               python3-nose
Standards-Version: 4.1.4
Vcs-Browser: https://github.com/lamyj/dicomifier
Vcs-Git: https://github.com/lamyj/dicomifier.git
Homepage: https://github.com/lamyj/dicomifier

Package: libdicomifier1
Architecture: any
Section: libs
Depends: ${shlibs:Depends}, ${misc:Depends}
Multi-Arch: same
Description: Convert data to and from DICOM (shared libraries)
 Dicomifier is a set of tools to convert Bruker to DICOM and DICOM to NIfTI. A
 set of mapping rules is provided, allowing the conservation of meta-data upon
 conversion.
 .
 This package contains the shared libraries.

Package: libdicomifier-dev
Architecture: any
Section: libdevel
Depends: libdicomifier1 (= ${binary:Version}),
         ${devlibs:Depends},
         ${misc:Depends}
Multi-Arch: same
Description: Convert data to and from DICOM (development files)
 Dicomifier is a set of tools to convert Bruker to DICOM and DICOM to NIfTI. A
 set of mapping rules is provided, allowing the conservation of meta-data upon
 conversion.
 .
 This package contains the development files.

Package: python-dicomifier
Architecture: any
Section: python
Depends: libdicomifier1 (= ${binary:Version}),
         python-dateutil, 
         python-numpy,
         python-odil (>= 0.9.2),
         ${shlibs:Depends}, 
         ${python:Depends},
         ${misc:Depends}
Multi-Arch: foreign
Description: Convert data to and from DICOM (Python modules)
 Dicomifier is a set of tools to convert Bruker to DICOM and DICOM to NIfTI. A
 set of mapping rules is provided, allowing the conservation of meta-data upon
 conversion.
 .
 This package contains the Python modules.

Package: python3-dicomifier
Architecture: any
Section: python
Depends: libdicomifier1 (= ${binary:Version}),
         python3-dateutil, 
         python3-nibabel,
         python3-numpy,
         python3-odil (>= 0.9.2),
         ${shlibs:Depends}, 
         ${python3:Depends},
         ${misc:Depends}
Multi-Arch: foreign
Description: Convert data to and from DICOM (Python 3 modules)
 Dicomifier is a set of tools to convert Bruker to DICOM and DICOM to NIfTI. A
 set of mapping rules is provided, allowing the conservation of meta-data upon
 conversion.
 .
 This package contains the Python 3 modules.

Package: dicomifier-cli
Architecture: all
Depends: python3-dicomifier, ${python3:Depends}, ${misc:Depends}
Provides: dicomifier
Conflicts: dicomifier
Replaces: dicomifier
Description: Convert data to and from DICOM (command line tools)
 Dicomifier is a set of tools to convert Bruker to DICOM and DICOM to NIfTI. A
 set of mapping rules is provided, allowing the conservation of meta-data upon
 conversion.
 .
 This package contains the command-line tools.
